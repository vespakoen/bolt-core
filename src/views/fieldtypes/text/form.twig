<div class="form-group{% if errors|length > 0 %} has-error{% endif %}">
    <input
        name="{{ key }}"
        class="form-control"
        type="text"
        value="{{ value }}">

    {% for error in errors %}
    <span class="help-block">{{ __(error) }}</span>
    {% endfor %}
</div>
